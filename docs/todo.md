# 템플릿 관련

* 또 moai는 opus 쓴건 없는지 확인해보라하기.

* mcp 호출이 사라졌는데 어떻게 도입할지 고민해보기. 그냥 놔두기가 아까움.

* 커맨드들 디스크립션도 읽어보고 수정할게 많음. 스펙.md 반영시킨 후에 파일들 하나하나 다 읽어야함. 잘못된것도 많음.

* context7 mcp 호출하는거, specify, plan, tasks, implement 단계에서 모두 명시 해야할듯?

* 코드 수정할 일 까지는 아니고, checklist 명령 수행해서 생성된 체크리스트는 codex한데 파악하라고 시키기.

* 분명히 했을텐데 implement한 후에 tasks.md 업데이트하라고. 한번 더 확인해보기.

* 마무리한 후에는, 이게 템플릿으로써 기능해야된다는 것을 알려주고 모든 workflow를 다 면밀하게 검토한 후에, 충돌하거나 모순되는 내용은 없는지 확인하라고 하기. 또 불필요한 file을 삭제할 수 있도록 알려달라고 하기. 또 이어서 README.md도 작성시켜야함. 단순하게 ms 워크플로우 외에 모든 file들을 검토 시켜야함.

* @IMMUTABLE 의 존재 자체를 제거해버릴 것. 이거때문에 불필요한 커맨드도 생겼음. 이거 있으면 에이전트가 해당 파일을 수정하지 않는다고 함.

* mcp__cli-bridge__gemini_cli 호출 이런 내용 찾아서 다 삭제해버리기. 지우라했는데 안 지웠나보네 시발롬이.

* 번역해서 하나하나 읽어보는 작업 마무리 되면 마스터 브랜치로 올리고 나머지 브랜치 다 삭제하고 커밋 내역까지 다 없애버리기.

* 아래 내용 확인해야함. 이게 안 되면 시발인데 왜 안 될까.
 🔴 CRITICAL 문제들
  1. 실행 불가능한 "병렬 에이전트" 로직
    - 명령어: "Launch 3 agents in TRUE PARALLEL with background=True"
    - 현실: 그런 파라미터 없음, Task tool은 순차 실행만 가능
    - 결과: 핵심 기능이 작동하지 않음



아래는 모두 완료함.

===
* 그리고 npx 설치법으로 바꾼 이후 에이전트들 로그인이 깨진다고. 근데 이건 테스트부터 해봐야함. codex는 잘 되고 있음. 제미나이는 확인을 해야함. 해결함.

* requirement.txt말고 더 힙한거 있었는데 poerty였나 그걸로 관리하도록 템플릿도 변경. <- 완료했음.


1. specify를 했는데 너무 상세했음. specify 딱 한 방에 컨텍스트 10% 남았음.
    이거 해결할 방법으로 터미널에서 cc 사용하면서, subagent로 코덱스랑 제미나이한데 분업을 시켜야할듯? specify 단계에서 context7까지 쓰기때문에 context7 읽고 전달하는건 코덱스한데 시키고 제미나이는 websearch 같은걸 시키거나, 백엔드는 코덱스, 프론트엔드는 제미나이 이렇게 시켜야할듯. 이렇게 하려면 MCP를 반드시 활용해야함. 그리고 프롬프트 캐싱 기법이 있던데 그거 적용가능한 영역인지도 확인할 것. 왜냐하면 plan이나 constitution tasks implements에서도 context7 또 읽고 있음.
2. plan 이후에 다음 스텝으로 tasks를 안내함 이거 수정.
3. sub agent는 별도의 file이나 설정이 없는데 어떻게 생성되고 역할이 부여되는건지 확인.
4. implement.md에다가 완료한 작업은 체크해서 tasks.md 업데이트를 반드시 하라고 지시.
5. implement에서 시간이 상당히 걸리는데 이유가?
6. 커밋한 이후에 linter나 formatter가 자동으로 파일들이 수정 되면서, /fin이나 /finq를 한 이후에도 git 상태가 깨끗하지 않고 남아있게 됨. 이것은 pre-commit hook이나 IDE의 auto-format 기능 때문일 수 있으므로, 한번에 처리 될 수 있도록 해결할 방법을 강구할 것.
      왜 이런 일이 발생했나요?
        1. 첫 번째 커밋 시도에서 pre-commit hook이 실패했습니다 (Ruff linter 경고)
        2. --no-verify 플래그로 hook을 우회하여 강제 커밋했습니다
        3. 하지만 VSCode/IDE나 다른 도구가 파일을 열거나 저장할 때 자동으로 포맷팅했습니다
        4. 결과적으로 커밋 후에 파일들이 수정된 상태로 남았습니다


* 깃 워크트리를 어케활용할지 포인트를 잡아보기. 동시에 같은 파일을 수정할 가능성이 있을때나 효과적으로 쓸수있을듯한데.
 - 아니고 동시에 여러 브랜치를 다룰때 효과적인거임. 서버 브랜치, db 브랜치 뭐 이런식. 나는 불필요함.

 - * pm cc할 때 시작부터 커밋 푸쉬하는게 적절한가?라는 의문이 있음. pull만 해오고 종료시점에 커밋 푸쉬해도 되지 않으까?

* mcp 도입 하나 남았음. 6개 에이전트 생성했고, 1,2,4번에다가는 코덱스, 제미나이 사용할 수 있다고 주석으로 표시해놨으니 추후 결정해서 주석 기반으로 찾아서 수정하라고 하면 될듯
제미나이는 방대한 컨텍스트 다루는데 강점이 있다고 함. 코덱스는 코드 전반에 대해 클로드코드와 비슷하다고 보면 될듯.
그래서 4,5,6번은 코덱스, 1,2번+ 아래 내용에 없는 Websearch는 제미나이로 된 구성이 가장 적절할듯. 3번은 그대로 클로드코드.

* requirement.txt가 makefile에 내용이 여전히 있음. 있어도될거 같긴한데 걍 제거하는게 나을듯.

* 시작 시점에 커밋 푸쉬는 해악이 크고 장점이 없음. 채찍피티 대화보고 개선하기
권장 패턴은 시작엔 “읽기 중심”(fetch/pull/stash), 종료엔 “쓰기 중심”(commit/push)

* MCP 해결했고 이제 아래 내용 그대로 던져주면 될듯.
===
1,2,4,5,6번 에이전트에 코덱스 내지는 제미나이를 sub agent의 툴로 사용할 수 있다는 메모가 주석으로 표기되어 있을텐데 4,5,6번은 코덱스, 1,2번의 내용은 제미나이에게 임무를 분담시킵니다. 어떻게 수정할거에요? 예시를 보여주세요. 해당 태스크를 수행할때는 반드시 그 에이전트만이 실행할 수 있도록 명확하게 지시하는 문구가 필요합니다.

---
  1. codebase-explorer.md - 코드베이스 패턴 검색
  2. library-researcher.md - Context7 MCP 라이브러리 조사
  3. integration-designer.md - 통합 설계
  4. constitution-extractor.md - Constitution 제약사항 추출
  5. trust-validator.md - TRUST 원칙 검증 (3단계 레벨)
  6. tag-auditor.md - TAG 추적성 감사
---
===

* 이 외에도 CHANGELOG.md를 제미나이나 코덱스한데 코드 변경시점마다 업뎃하라는 내용 임플리먼트에 넣고, 리드미는 finq에서 주요변경사항이 있는걸 포착하면 바꾸는데, 변경 내역을 추적할 수 있도록 수정할 필요가 없으니 그냥 최신화하라하기. (변경됨) 이런 멘트가 필요없다는 말을 어떻게 명시적으로 전달할지 모르겠네.



---

아래 이거 좀 수정해야함. 일단 moai 계층적으로 분석하라고 하고, spec.md에다가, 4개 기능을 순차적으로 추가하게 만들어야겠음. 한번에 다 시키면 제대로 못할 것 같음. 일단 moai가 해당 기능을 어떻게 구현하고 있는지 뜯어서 분석한 후에, skills를 도입시킬때는 skills 디렉도 같이 분석하고, 나머지 기능은 moai를 다시 분석하면서 해당 기능을 도입시키도록 하기. 이미 작성된 파일이 있으니 그것도 참고 시키고 spec.md에 덧붙이라고 할 것. 나머지 문구는 크게 수정할 것이 없을듯.

MoAI에서, Skills, Living-Docs, Hooks, Sub-Agents 이 4개 기능을 우리의 워크플로우에 맞게 최적화 시켜 이식할 것입니다. 이를 위해 우리의 워크플로우는 @.claude/commands/ms.*.md 형태로 존재하고 있으니 우리 워크플로우에 대한 디테일한 분석이 필요합니다. 또한 @docs/reference/moai-adk/ 이 moai 디렉토리를 함께 분석해서 moai가 해당 기술들을 어떤 방식으로 계층적으로 적용하고 있는지 완벽하게 분석하세요. 그리고 루트 디렉토리에 해당 내용을 spec.md로 만들어서 작성하세요. Skills, Living-Docs, Hooks, Sub-Agents 4개 기능을 개별 브랜치로 만들어서 순차적으로 적용할 것인데, 그 순서를 정해주세요. 순서가 상관이 없거나, 동시에 통합개발하는 것이 낫다면 그렇게 하라고 제안을 해주세요.

---
MoAI에서, Living-Docs 기능을 우리의 워크플로우에 맞게 최적화 시켜 이식할 것입니다. 우리의 워크플로우는 @.claude/commands/ms.*.md 형태로 존재하고 있으니 이 워크플로우에 대한 디테일한 분석이 필요합니다. @docs/reference/moai-adk/ 이 moai 디렉토리를 함께 분석해서 moai가 Living-Docs 기능을 어떻게 적용하여 활용하고 있는지 기술하고, Living-Docs 기능의 도입을 위한 명세서를 루트 디렉토리에 spec-living-docs.md 파일에 작성하세요. 
명세서는 prd, trd와 같은 개념이 아니라, 문자 그대로 어떤 기능을 도입할지 이유와 함께 제시하고, 이를 위해 어떤 file을 생성할 것이고, 또는 어떤 file이 변경될 것이며, 최종적으로 어떤 효과가 기대되는지를 정리. moai의 기능을 우리 워크플로우에 도입하면서, 경로의존성과 같은 것들을 그대로 이식해버려 우리 프로젝트에서는 제대로된 사용이 불가능한 일이 생기지 않도록 주의. 이 외에도 다른 프로젝트의 기능을 이식해올 때 흔히 발생할 수 있는 문제들을 사전에 고려하여 실수하지 않도록 기본 룰을 정할 것.


---
* 또 얼마전에 뜬 SKILLS 이것도 도입을 고려해봐야함.

* MoAI 또 클론해서 서브에이전트랑 skills 훔치기.

* 아래처럼 서브에이전트를 쓰는데, 이것도 니맘대로 만드는 에이전트 말고 표준화된 에이전트를 쓰자고 정리하기.
  적응형 Sub-Agent 전략
  - SIMPLE: 0 agents (직접 처리)
  - MODERATE: 2 agents (병렬 실행)
  - COMPLEX: 3 agents (병렬 실행)

* 코덱스나 제미나이한데 짬때릴만한 것들 더 훑어보기.

* 병렬은 잘 되고 있는데, 브릿지 mcp의 제미나이가 context7 mcp를 이중으로 불러오는 구조가 실패하고 있음. 이를 위해서 context7 mcp를 아예 프로젝트 루트에 설치했을때는 아주 잘 됐음. 그런데 이거는 개헛짓거리임. 호스트에 context7 mcp를 설치하는 정도는 봐줄 수 있는데 방법을 찾아보기. api를 써서 불러오자니, mcp를 쓰면 라이브러리 이름을 알아서 찾아서 하는 반면에 api를 쓰면 그 라이브러리 이름을 정확히 찾아서 지정해줘야하는 구조 같음. 그래서 mcp를 써야할듯 함.

* 스페시파이 플랜 태스크 할 때 모두나 특정 단계에서 기존 moai가 구현한 방식을 참고하라는 말을 던져줘야할듯? 다만, 다른 프로젝트의 기능을 이식해올 때 흔히 발생할 수 있는 문제들을 답습하는 일을 하지말라고하고.
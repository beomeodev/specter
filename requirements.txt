# ============================================
# My-Spec 프로덕션 의존성
# 실제 운영 환경에서 필요한 패키지만 포함
# ============================================

# --- 데이터 처리 ---
PyYAML>=6.0.1              # YAML 파일 파싱 (spec.md front matter)
regex>=2024.0.0            # EARS/TAG 패턴 매칭

# ============================================
# 설치 방법
# ============================================
# 프로덕션: pip install -r requirements.txt
# 개발 환경: pip install -r requirements.txt -r requirements-dev.txt
